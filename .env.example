# ============================================================
# 前端环境变量示例（Vite）
# 复制为 .env.production（生产构建时自动加载）或 .env.local（本地覆盖，不提交）
# Vite 只暴露以 VITE_ 开头的变量给前端代码。
# ============================================================

# ---------- 后端 API 地址 ----------
# 【同源部署】前端静态文件与后端 API 由同一个 Nginx 提供（/api 反向代理到后端）
#             此时无需设置本项，代码默认走相对路径 '/api'，Cookie 同源最省事。
# VITE_API_BASE_URL=/api

# 【分离部署】前端在 CDN/静态托管、后端独立域名时，填后端完整地址：
#             注意必须带协议，跨域需后端 CORS_ORIGIN 同时放行，且 Cookie 需 HTTPS。
# VITE_API_BASE_URL=https://api.example.com/api

# ---------- 本地开发代理目标（可选）----------
# 仅本地开发时 Vite 代理指向的后端地址，默认 http://127.0.0.1:7001
# VITE_PROXY_TARGET=http://127.0.0.1:7001
